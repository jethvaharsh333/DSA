FIND FREQUENCY OF EACH NUMBER

We are given an array of positive integers(a[n]) .
We are given multiple queries of the form : [x] which means we need to output the frequency of integer 'x' in the array.
All numbers inside the array are from range [0,N-1].